<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content" android:padding="2dp"
    android:foregroundTint="@color/colorAccent">

    <TextView
        android:id="@+id/message_thread_bubble"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="5dp"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="2dp"
        android:background="@drawable/message_box"
        android:fontFamily="@font/amaranth"
        android:minWidth="200dp"
        android:minHeight="40dp"
        android:maxWidth="300dp"
        android:padding="8dp"
        android:text="this is sample text"
        android:textSize="16dp" />

    <TextView
        android:id="@+id/spam_symbol_thread"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="10dp"
        android:layout_toStartOf="@+id/unread_count_thread"
        android:background="@drawable/rounded_text_small"
        android:backgroundTint="@color/colorAccent"
        android:gravity="center"
        android:textColor="@android:color/white"
        android:textSize="5dp" />

    <TextView
        android:id="@+id/unread_count_thread"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="10dp"
        android:layout_marginEnd="25dp"
        android:background="@drawable/rounded_text_small"
        android:backgroundTint="@color/colorPrimary"
        android:gravity="center"
        android:textColor="@android:color/white"
        android:textSize="5dp" />
</RelativeLayout>